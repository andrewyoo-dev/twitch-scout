# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Local SQLite sample store (data is kept in Turso, not the repo)
*.db
*.db-wal
*.db-shm

# Environment / secrets — never commit Twitch or Turso credentials
.env
.env.*

# Local review workspace: review-feedback and per-review reports are transient handoff
# notes between Claude and Astra. Confirmed fixes live in commits/tests, durable
# decisions in docs/DECISIONS.md. A fresh clone does not have this directory.
reviews/
